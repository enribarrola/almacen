class tipoartlog
{
    tipoartDAO obj = new tipoartDAO();
    public void agregar()
    {  /*
        if (obj.agregar()))
        {
            MessageBox.Show("Guardado correctamente", "Almacen", MessageBoxButtons.OK);

        };*/

    }

}